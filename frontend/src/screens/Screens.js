import React from "react";
import { Button } from "@material-ui/core";

export default function Screens() {
    return (
        <div>
            <Button variant={ 'contained' } color={ 'primary' }>dasdsa</Button>
        </div>
    )
}
